# Express server port (default 3001).
# Used by both `npm run dev` and `npm start`.
PORT=3001

# Optional: where the Vite dev server proxies /api/* to.
# Only set this if you're running the proxy on a different host/port.
# VITE_PROXY_TARGET=http://localhost:3001
